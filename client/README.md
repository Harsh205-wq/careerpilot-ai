# CareerPilot AI Client

This folder contains the static client application for CareerPilot AI.

## Run locally

Open `index.html` in a browser or serve the folder using a static file server.

## Structure

- `index.html` — entry point for the client UI.
- `style.css` — styles for the client.
- `app.js` — simple client-side JavaScript.
